# clojure-tdd
A simple To-Do List system implemented in Clojure using Test-Driven Development (TDD). Demonstrates how to build clean and maintainable CRUD functionality guided entirely by automated unit tests. Includes task creation, editing, deletion, status transitions, and validation.
